package com.ahmadkharfan.androidstudiolite.build.common

/**
 * Skeleton marker for :build:common — shared, platform-neutral build-pipeline types used by both
 * flavor backends. Real types land with the build-engine work (T8/T9).
 */
const val BUILD_COMMON_MODULE = "build-common"
