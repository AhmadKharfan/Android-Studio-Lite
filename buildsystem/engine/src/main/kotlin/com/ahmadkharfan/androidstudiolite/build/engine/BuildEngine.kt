package com.ahmadkharfan.androidstudiolite.build.engine

/**
 * Skeleton marker for :build:engine — the play-flavor in-process build engine (cleanroom).
 * Real pipeline lands with T9.
 */
const val BUILD_ENGINE_MODULE = "build-engine"
