package com.ahmadkharfan.androidstudiolite.tooling.proto

/**
 * Skeleton marker for :tooling:proto — the JSON-RPC protocol shared by the app and the on-device
 * tooling server. Real protocol lands with T10.
 */
const val TOOLING_PROTO_MODULE = "tooling-proto"
