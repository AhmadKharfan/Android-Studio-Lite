package com.ahmadkharfan.androidstudiolite.feature.onboarding.common

/** Onboarding is permissions → done; the Setup step went with the on-device toolchain install. */
val ONBOARDING_STEPS = listOf("Permissions", "Done")
